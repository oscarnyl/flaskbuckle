version: "3.3"
services:
    app:
        build: .
        ports:
            - "0.0.0.0:5000:5000"
        volumes:
            - ./src:/app/src
        environment:
            - FLASK_APP=/app/src/app.py
            - FLASK_DEBUG=1
